services:
  nginx-rtmp:
    build: ./server/nginx-rtmp
    ports:
      - "1935:1935"   # RTMP ingest (câmeras)
      - "8080:8080"   # HLS playback + stats
    volumes:
      - hls_data:/data/hls
      - recordings:/data/recordings
    depends_on:
      - api
    restart: unless-stopped

  api:
    build: ./server/api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://happydo:happydo@db:5432/happydo_guard
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - RTMP_HOST=nginx-rtmp
      - HLS_BASE_URL=http://nginx-rtmp:8080/hls
      - PORT=8000
      - PULSE_API_URL=${PULSE_API_URL:-https://happydopulse-production.up.railway.app/api}
      - PULSE_EMAIL=${PULSE_EMAIL:-}
      - PULSE_PASSWORD=${PULSE_PASSWORD:-}
      - FACE_SERVICE_URL=http://face-service:8001
    volumes:
      - recordings:/data/recordings
      - ./deploy-status.json:/app/deploy-status.json:ro
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  face-service:
    build: ./server/face-service
    ports:
      - "8001:8001"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    depends_on:
      - api
      - nginx-rtmp
    restart: unless-stopped

  db:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_USER=happydo
      - POSTGRES_PASSWORD=happydo
      - POSTGRES_DB=happydo_guard
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U happydo -d happydo_guard"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  hls_data:
  recordings:
